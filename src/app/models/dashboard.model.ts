export interface DashboardCounts {
  computers: number;
  mice: number;

  activeLoans: number;
  overdueLoans: number;

  totalStudents: number;

  totalCases: number;
  openCases: number;
  closedCases: number;
}